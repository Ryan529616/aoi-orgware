# AOI source snapshot

This first repository revision preserves a sanitized snapshot of the harness
that motivated AOI. It contains only implementation source, tests, policy, and
templates. Runtime state, task records, claims, sessions, logs, evidence, and
machine-specific paths are intentionally excluded.

AOI will be generalized in the next revision as **Agent Organization
Infrastructure**, an experimental orgware layer for governed multi-agent
engineering work.

