# Onboarding sample (not evaluation evidence)

This intentionally broken project exists only to rehearse the pilot procedure.
Do not include its result in AOI effectiveness claims.

Run the oracle:

```bash
python -m unittest discover -s . -p 'test_*.py' -v
```
