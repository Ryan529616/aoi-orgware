# AOI pilot example

Generate the maintained tester kit from the installed package:

```bash
aoi pilot-init --output ./aoi-pilot-kit --json
```

The generated kit is canonical; this directory intentionally does not duplicate
its templates. See [`docs/PILOT.md`](../../docs/PILOT.md) for the coordinator
guide.
